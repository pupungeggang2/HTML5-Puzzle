function loopTitleMenu() {
    displayTitleMenu()
}

function displayTitleMenu() {
    drawSceneInit()
    context.fillText(`Puzzle`, UI.titleMenu.titleText[0], UI.titleMenu.titleText[1])
}

function mouseUpTitleMenu(x, y, button) {
    if (button === 0) {
        if (menu === false) {
            if (state === '') {
                
            }
        }
    }
}
